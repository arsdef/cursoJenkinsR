pipeline {     agent any     stages     {         stage("Comando de CMD")         {             steps             {                 bat 'java -version'                 bat 'git --version'             }         }     }     }     }    }
pipeline
{
	agent any
	stages
	{
		stage(comandos)
		{
			steps
			{
				sh 'java --versio'
				sh 'git --version
			}
		}
	}


}
